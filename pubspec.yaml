name: cardio_predict_mobile_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.5.0
  flutter_vector_icons: ^2.0.0

  # AJOUTEZ CES DEUX POUR LES PDFs
  pdf: ^3.10.9
  printing: ^5.11.2
  
  # Pour enregistrer les fichiers
  path_provider: ^2.1.3
  open_file: ^3.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/